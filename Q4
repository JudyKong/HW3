background(255);
size(500,500);

int x=20;
while(x<480){
  int y=20;
 
    noStroke();
    colorMode( HSB, 100);
    for( int i=0; i<70; i=i+3){
      fill(i, 100, 100);
      ellipse( x,y,18,18);
      y=y+20;
    }
    x=x+20;
}
